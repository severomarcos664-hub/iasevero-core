# IASevero v252.0 — Runtime Event Flow Audit

## Objetivo

Mapear o fluxo real de eventos do runtime da IASevero com base em evidências do código-fonte.

## Evidências principais

A rota principal identificada é:

- app/api/chat/route.ts

O fluxo operacional confirmado inclui:

- runRuntimeExecutionBridge()
- createRuntimeTaskPlan()
- executeRuntimePipeline()
- updateTaskStatus()
- emitRuntimeEvent()
- getQueueMetrics()
- getRuntimeEvents()
- processRuntimeEvent()

## Fluxo confirmado

```text
POST /api/chat
        ↓
RuntimeExecutionBridge
        ↓
RuntimeTaskPlanner
        ↓
RuntimeExecutionPipeline
        ↓
QueueGovernor
        ↓
RuntimeEventBus
        ↓
RuntimeCoordinator
        ↓
RuntimeEventProcessor
        ↓
Runtime Processing Report

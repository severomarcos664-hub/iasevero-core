const results = commands.map(cmd => simulateCommand(cmd))

const validation = validateQuality(reply, commands)

if (!validation.passed) {
  reply = improveReply(reply, commands)
}

return {
  intent,
  reply,
  commands,
  execution: results,
  validation
}

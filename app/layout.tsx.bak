export const metadata = {
  title: "IASevero",
  description: "Neural Interface Omega Core",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="pt-BR">
      <body style={{
        margin: 0,
        padding: 0,
        background: "#04060d",
      }}>
        {children}
      </body>
    </html>
  );
}

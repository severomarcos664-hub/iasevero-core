import {
  createRuntimeToolRegistry,
} from '../app/lib/runtime-core/runtime-tool-registry'

const registry = createRuntimeToolRegistry()

console.log('\n=== IASEVERO RUNTIME TOOL REGISTRY ===\n')
console.log(registry)
